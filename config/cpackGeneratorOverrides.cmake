if(CPACK_GENERATOR MATCHES "^(7Z|TBZ2|TGZ|TXZ|TZ|TZST|ZIP)$")
    set(CPACK_INCLUDE_TOPLEVEL_DIRECTORY NO)
endif()

if (CPACK_GENERATOR STREQUAL "DEB")
    set(CPACK_DEBIAN_PACKAGE_MAINTAINER "maintainer@denoming.com")
    set(CPACK_DEBIAN_PACKAGE_DEPENDS "libstdc++-10-dev (>= 10.3.0)")
    set(CPACK_DEBIAN_PACKAGE_HOMEPAGE "https://www.denoming.com")
    set(CPACK_DEBIAN_FILE_NAME DEB-DEFAULT)
    if (CMAKE_BUILD_TYPE STREQUAL "Debug" OR CMAKE_BUILD_TYPE STREQUAL "RelWithDebInfo")
        set(CPACK_DEBIAN_DEBUGINFO_PACKAGE ON)
    endif()
endif()